# Caddy

Reverse-proxy and TLS configuration for low-cost single-node deployments will live here.

Only public HTTP(S) entry points should be exposed. Database, cache, telemetry, and internal administrative ports remain private.
