# Docker Compose

Compose files for local, development, and demo deployment profiles will live here.

The compose stack should remain suitable for a single inexpensive host and use the same immutable application images promoted through later environments.
