# DEV edge configuration example. Runtime values are materialized on the target host.
IAM_PUBLIC_HOST=iam.example.com
IAM_ACME_EMAIL=ops@example.com
IAM_SERVER_UPSTREAM=server:8080
IAM_CONSOLE_UPSTREAM=console:8080
