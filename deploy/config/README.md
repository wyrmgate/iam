# Deployment Configuration

Non-secret environment configuration templates live here.

Secrets, credentials, signing keys, cloud tokens, and private material must be injected at runtime and must never be committed.
