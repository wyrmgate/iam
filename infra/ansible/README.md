# Ansible

Host bootstrap and operating-system configuration live here.

Expected responsibilities include package updates, users/SSH hardening, Docker installation, firewall rules, IAM directories, monitoring agents, and backup jobs.

Application deployment remains separate from host provisioning.
