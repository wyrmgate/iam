# OpenTofu

Declarative cloud/network/compute/storage/DNS provisioning lives here.

Provider-specific modules must remain infrastructure concerns and must not leak into the IAM domain. Environment state and credentials must never be committed.
