package io.wyrmgate.iam.identity.domain;

/** Governed scalar types supported by canonical extension attributes. */
public enum CanonicalAttributeType {
    STRING,
    BOOLEAN,
    INTEGER,
    DECIMAL,
    DATE,
    DATETIME,
    ENUM
}
